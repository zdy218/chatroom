const BASE_URL = 'localhost'
const WS_PORT = '8000'
const localhost = 'http://127.0.0.1:3000'
export const WS_ADDRESS = `ws://${BASE_URL}:${WS_PORT}`
export const uploadUrl = `${localhost}/images/`
export const fileIconUrl = `${localhost}/fileicon/`
export const fileuploadUrl = localhost